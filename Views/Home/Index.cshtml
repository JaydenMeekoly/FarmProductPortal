@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center mt-5">
    <h1 class="display-4 mb-4">Welcome to FarmProductPortal</h1>
    <p class="lead mb-5">Choose your login path below:</p>

    <div class="d-flex justify-content-center gap-3">
        <a class="btn btn-success btn-lg px-5" href="@Url.Action("Index", "Products")">
            Login as Farmer
        </a>
        <a class="btn btn-primary btn-lg px-5" href="@Url.Action("Index", "Farmers")">
            Login as Employee
        </a>
    </div>
</div>
